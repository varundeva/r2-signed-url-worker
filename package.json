{
  "name": "r2-signed-url-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that provides signed, expiring URLs for private R2 objects",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.25.0"
  },
  "license": "MIT"
}
